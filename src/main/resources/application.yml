server:
  port: 8080

spring:
  profiles:
    active:
  datasource:
    url: jdbc:mysql://localhost:3306/oyh_store?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 980707
  jackson:
    default-property-inclusion: non_null

mybatis:
  mapper-locations: classpath:mappers/*.xml

log:
  path: ./logs
  level: info



# 日志文件配置---> 转向xml自定义配置
#logging:
#  file:
#    name: oyh-store-log.log
#    max-size: 10MB
#  level:
#    com.loveoyh.store: DEBUG
#  pattern:
#    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{150} - %msg%n"
#    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{150} - %msg%n"